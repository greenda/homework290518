import { MenuItemsPipe } from './menu-items.pipe';

describe('MenuItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
